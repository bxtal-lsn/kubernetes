# Kubernetes Applications

This directory contains Helm charts for deploying applications to your Kubernetes cluster.

## Available Applications

- **rqlite**: Distributed SQLite database (3 replicas by default)

## Usage

### Prerequisites

1. Ensure you have Helm installed:
   ```bash
   curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
